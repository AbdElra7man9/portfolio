import { FC } from 'react'
import { cva } from "class-variance-authority";

interface ParaghraphProps {
  
}

const Paraghraph: FC<ParaghraphProps> = ({}) => {
  return <div>Paraghraph</div>
}

export default Paraghraph